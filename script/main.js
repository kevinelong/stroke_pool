/**
 * CREATE GAME INSTANCE
 */


defer(function () {

    // for (b = 0; b <= 15; b++) {
    //     append(getBallElement(b));
    // }

    //d.appendChild(s);

    new StrokePool(
        get('stroke_pool')
    );
});

